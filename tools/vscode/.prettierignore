node_modules/
dist/
*.vsix
package-lock.json

# Prose is hand-formatted to match the rest of the repo's docs (which have no
# prettier); leave it alone. Prettier owns code + config here, not markdown.
*.md
media/js/
out/
media/codicons/

# Generated by esbuild.mjs from devicon, like media/codicons/ above. Reformatting it
# would make every build a diff, since the generator writes it back its own way.
src/shared/langGlyphs.ts
